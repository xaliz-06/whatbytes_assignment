export type InputType = {
  name: string;
  value: number;
};

export type PercentileType = {
  percentile: number;
  noOfStudents: number;
};

export type UpdateDetails = {
  rank: number;
  percentile: number;
  score: number;
};
